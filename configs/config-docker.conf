# Add flags to the 'docker run' command used by compile.sh when running inside a container.
# See https://docs.armbian.com/Developer-Guide_Building-with-Docker/

# Uncomment the following line to enable privileged mode, often needed for certain operations like Flatpak builds or direct device access.
DOCKER_FLAGS+=(--privileged)

# Example: Add other flags if needed
# DOCKER_FLAGS+=("--cap-add=SYS_ADMIN")
